@model Cinema.Models.MovieActor
@{
    ViewData["Title"] = "Create";
}

<h2>Create Movie-Actor Relation</h2>
<form method="post" asp-action="Add" asp-controller="MovieActor">
    <div class="form-group">
        <label asp-for="MovieId"></label>
        <input asp-for="MovieId" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="ActorId"></label>
        <input asp-for="ActorId" asp-items=
        "@(new SelectList(ViewBag.Genres,"Title"))"
               class="form-control" />
    </div>

    <button type="submit" class="btn btn-primary">Create</button>
</form>